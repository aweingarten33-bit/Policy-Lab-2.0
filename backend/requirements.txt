# Pinned for reproducible builds.
#
# These were previously open-ended (>=), which meant every container rebuild
# could silently pull different — potentially breaking or vulnerable — versions
# with no change to the repo. The versions below are the exact set verified
# working against this codebase.
#
# To upgrade: bump deliberately, reinstall, run the app, and commit the new pin.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
litellm==1.98.0
python-docx==1.2.0
pdfplumber==0.11.10
PyPDF2==3.0.1
chromadb==1.5.9
httpx==0.28.1
APScheduler==3.11.3
aiofiles==25.1.0
